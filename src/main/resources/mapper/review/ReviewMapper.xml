<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    리뷰 통합 Mapper XML.
    테이블: reviews, review_likes, review_votes
    담당: 이민수 (review 도메인) + admin 동적 검색은 윤형주 유지보수

    설계:
      - @Entity는 DDL 정의 전용, 데이터 R/W는 100% 이 Mapper (§15)
      - Review는 String userId + @Transient String nickname 구조
      - 목록/상세는 JOIN users로 nickname 로드
      - 엑셀 5번 reviews DB 컬럼명 반영: contents, is_spoiler, review_category_code
-->
<mapper namespace="com.monglepick.monglepickbackend.domain.review.mapper.ReviewMapper">

    <!-- ═══ ResultMaps ═══ -->

    <!-- 리뷰 기본 매핑 (nickname 없음) -->
    <resultMap id="reviewResultMap" type="com.monglepick.monglepickbackend.domain.review.entity.Review">
        <id property="reviewId" column="review_id"/>
        <result property="userId" column="user_id"/>
        <result property="movieId" column="movie_id"/>
        <result property="rating" column="rating"/>
        <result property="content" column="contents"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="isBlinded" column="is_blinded"/>
        <result property="spoiler" column="is_spoiler"/>
        <result property="likeCount" column="like_count"/>
        <result property="reviewSource" column="review_source"/>
        <result property="reviewCategoryCode" column="review_category_code"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedBy" column="updated_by"/>
    </resultMap>

    <!-- 리뷰 + JOIN users.nickname 매핑 -->
    <resultMap id="reviewWithNicknameResultMap" type="com.monglepick.monglepickbackend.domain.review.entity.Review"
               extends="reviewResultMap">
        <result property="nickname" column="nickname"/>
    </resultMap>

    <!-- 리뷰 좋아요 매핑 -->
    <resultMap id="reviewLikeResultMap" type="com.monglepick.monglepickbackend.domain.review.entity.ReviewLike">
        <id property="reviewLikeId" column="review_like_id"/>
        <result property="reviewId" column="review_id"/>
        <result property="userId" column="user_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 리뷰 투표 매핑 -->
    <resultMap id="reviewVoteResultMap" type="com.monglepick.monglepickbackend.domain.review.entity.ReviewVote">
        <id property="reviewVoteId" column="review_vote_id"/>
        <result property="userId" column="user_id"/>
        <result property="reviewId" column="review_id"/>
        <result property="helpful" column="helpful"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- ═══ 공통 SELECT 컬럼 ═══ -->

    <sql id="reviewColumns">
        r.review_id, r.user_id, r.movie_id, r.rating, r.contents,
        r.is_deleted, r.is_blinded, r.is_spoiler, r.like_count,
        r.review_source, r.review_category_code,
        r.created_at, r.updated_at, r.created_by, r.updated_by
    </sql>

    <!-- ═══ Review 단건 조회 ═══ -->

    <select id="findById" resultMap="reviewResultMap">
        SELECT <include refid="reviewColumns"/>
        FROM reviews r
        WHERE r.review_id = #{reviewId}
    </select>

    <select id="existsById" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM reviews
        WHERE review_id = #{reviewId}
    </select>

    <select id="isDeletedById" resultType="java.lang.Boolean">
        SELECT is_deleted
        FROM reviews
        WHERE review_id = #{reviewId}
    </select>

    <select id="findByIdWithNickname" resultMap="reviewWithNicknameResultMap">
        SELECT <include refid="reviewColumns"/>,
               u.nickname AS nickname
        FROM reviews r
        LEFT JOIN users u ON r.user_id = u.user_id
        WHERE r.review_id = #{reviewId}
    </select>

    <!-- ═══ Review 목록 조회 ═══ -->

    <select id="findByMovieIdWithNickname" resultMap="reviewWithNicknameResultMap">
        SELECT <include refid="reviewColumns"/>,
               u.nickname AS nickname
        FROM reviews r
        LEFT JOIN users u ON r.user_id = u.user_id
        WHERE r.movie_id = #{movieId}
          AND r.is_deleted = false
        ORDER BY r.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByMovieId" resultType="long">
        SELECT COUNT(*)
        FROM reviews
        WHERE movie_id = #{movieId}
          AND is_deleted = false
    </select>

    <select id="findByUserId" resultMap="reviewResultMap">
        SELECT <include refid="reviewColumns"/>
        FROM reviews r
        WHERE r.user_id = #{userId}
          AND r.is_deleted = false
        ORDER BY r.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM reviews
        WHERE user_id = #{userId}
          AND is_deleted = false
    </select>

    <select id="existsByUserIdAndMovieId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM reviews
        WHERE user_id = #{userId}
          AND movie_id = #{movieId}
    </select>

    <!--
        활성 리뷰 단건 조회 (UPSERT 분기용).
        is_deleted=false 조건으로 소프트 삭제된 리뷰는 제외.
        ReviewService.createOrUpdateFromRecommendation 에서 사용.
    -->
    <select id="findByUserIdAndMovieId" resultMap="reviewResultMap">
        SELECT <include refid="reviewColumns"/>
        FROM reviews r
        WHERE r.user_id = #{userId}
          AND r.movie_id = #{movieId}
          AND r.is_deleted = false
        LIMIT 1
    </select>

    <!--
        (user_id, movie_id IN (...)) 활성 리뷰 배치 조회 — 추천 이력 페이지 별점 복원용.
        movieIds 가 비어있으면 IN () 구문 SQL 오류 방지를 위해 빈 결과 반환.
        is_deleted=false 조건. 호출부(RecommendationHistoryService)는 동일 페이지 안에서
        같은 movie_id 가 여러 번 나올 수 있으나(같은 영화가 다른 추천 로그로 재추천되는 경우)
        reviews 는 (userId, movieId) 단일 활성 리뷰 정책이라 결과는 movieId 별 최대 1건.
    -->
    <select id="findByUserIdAndMovieIds" resultMap="reviewResultMap">
        <if test="movieIds == null or movieIds.size() == 0">
            SELECT <include refid="reviewColumns"/>
            FROM reviews r
            WHERE 1 = 0
        </if>
        <if test="movieIds != null and movieIds.size() > 0">
            SELECT <include refid="reviewColumns"/>
            FROM reviews r
            WHERE r.user_id = #{userId}
              AND r.is_deleted = false
              AND r.movie_id IN
              <foreach collection="movieIds" item="mid" open="(" separator="," close=")">
                  #{mid}
              </foreach>
        </if>
    </select>

    <!-- ═══ Review 쓰기 ═══ -->

    <insert id="insert" useGeneratedKeys="true" keyProperty="reviewId">
        INSERT INTO reviews (
            user_id, movie_id, rating, contents,
            is_deleted, is_blinded, is_spoiler, like_count,
            review_source, review_category_code,
            created_at, updated_at, created_by, updated_by
        ) VALUES (
            #{userId}, #{movieId}, #{rating}, #{content},
            false, false, #{spoiler}, #{likeCount},
            #{reviewSource}, #{reviewCategoryCode},
            NOW(), NOW(), #{createdBy}, #{updatedBy}
        )
    </insert>

    <update id="update">
        UPDATE reviews
        SET rating     = #{rating},
            contents   = #{content},
            updated_at = NOW(),
            updated_by = #{updatedBy}
        WHERE review_id = #{reviewId}
    </update>

    <update id="softDelete">
        UPDATE reviews
        SET is_deleted = true,
            updated_at = NOW()
        WHERE review_id = #{reviewId}
    </update>

    <update id="restore">
        UPDATE reviews
        SET is_deleted = false,
            updated_at = NOW()
        WHERE review_id = #{reviewId}
    </update>

    <update id="blind">
        UPDATE reviews
        SET is_blinded = true,
            updated_at = NOW()
        WHERE review_id = #{reviewId}
    </update>

    <update id="unblind">
        UPDATE reviews
        SET is_blinded = false,
            updated_at = NOW()
        WHERE review_id = #{reviewId}
    </update>

    <delete id="deleteById">
        DELETE FROM reviews
        WHERE review_id = #{reviewId}
    </delete>

    <!-- ═══ Review 통계 ═══ -->

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM reviews
    </select>

    <select id="countByCreatedAtBetween" resultType="long">
        SELECT COUNT(*)
        FROM reviews
        WHERE created_at >= #{start}
          AND created_at &lt; #{end}
    </select>

    <select id="findAverageRating" resultType="java.lang.Double">
        SELECT AVG(rating)
        FROM reviews
        WHERE rating IS NOT NULL
          AND is_deleted = false
    </select>

    <select id="findAverageRatingBetween" resultType="java.lang.Double">
        SELECT AVG(rating)
        FROM reviews
        WHERE rating IS NOT NULL
          AND is_deleted = false
          AND created_at BETWEEN #{start} AND #{end}
    </select>

    <!-- ═══ Review 관리자 동적 검색 ═══ -->

    <sql id="adminReviewWhere">
        WHERE 1 = 1
        <if test="movieId != null and movieId != ''">
            AND r.movie_id = #{movieId}
        </if>
        <if test="minRating != null">
            AND r.rating >= #{minRating}
        </if>
        <!--
            도장깨기 인증 리뷰 모니터링 — review_category_code 필터.
            enum.name() 문자열 비교 (THEATER_RECEIPT/COURSE/WORLDCUP/WISHLIST/AI_RECOMMEND/PLAYLIST)
        -->
        <if test="categoryCode != null and categoryCode != ''">
            AND r.review_category_code = #{categoryCode}
        </if>
    </sql>

    <select id="searchAdminReviews" resultMap="reviewWithNicknameResultMap">
        SELECT <include refid="reviewColumns"/>,
               u.nickname AS nickname
        FROM reviews r
        LEFT JOIN users u ON r.user_id = u.user_id
        <include refid="adminReviewWhere"/>
        ORDER BY r.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countAdminReviews" resultType="long">
        SELECT COUNT(*)
        FROM reviews r
        <include refid="adminReviewWhere"/>
    </select>

    <select id="findAllAdminReviews" resultMap="reviewWithNicknameResultMap">
        SELECT <include refid="reviewColumns"/>,
               u.nickname AS nickname
        FROM reviews r
        LEFT JOIN users u ON r.user_id = u.user_id
        ORDER BY r.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- ═══ 내 리뷰 목록 조회 — 고객센터 AI 봇 진단용 (v4 신규, 2026-04-28) ═══ -->

    <!--
        findMyReviewsWithTitle:
          reviews LEFT JOIN movies — 영화 제목을 한 번에 가져온다.
          소프트 삭제(is_deleted=true) 제외. created_at DESC 정렬.
          since 파라미터: reviews.created_at >= since (LocalDateTime)
    -->
    <select id="findMyReviewsWithTitle"
            resultType="com.monglepick.monglepickbackend.domain.review.dto.MyReviewSummaryRow">
        SELECT r.review_id   AS reviewId,
               r.movie_id    AS movieId,
               m.title       AS movieTitle,
               r.rating      AS rating,
               r.contents    AS content,
               r.created_at  AS createdAt
        FROM reviews r
        LEFT JOIN movies m ON r.movie_id = m.movie_id COLLATE utf8mb4_unicode_ci
        WHERE r.user_id    = #{userId}
          AND r.is_deleted = false
          AND r.created_at >= #{since}
        ORDER BY r.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!--
        countMyReviewsSince:
          페이징 total 계산용 카운트 쿼리.
          소프트 삭제(is_deleted=true) 제외.
    -->
    <select id="countMyReviewsSince" resultType="long">
        SELECT COUNT(*)
        FROM reviews
        WHERE user_id    = #{userId}
          AND is_deleted = false
          AND created_at >= #{since}
    </select>

    <!-- ═══ ReviewLike ═══ -->

    <select id="findReviewLikeByReviewIdAndUserId" resultMap="reviewLikeResultMap">
        SELECT review_like_id, review_id, user_id, created_at, updated_at
        FROM review_likes
        WHERE review_id = #{reviewId}
          AND user_id = #{userId}
        LIMIT 1
    </select>

    <select id="existsReviewLikeByReviewIdAndUserId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM review_likes
        WHERE review_id = #{reviewId}
          AND user_id = #{userId}
    </select>

    <select id="countReviewLikeByReviewId" resultType="long">
        SELECT COUNT(*)
        FROM review_likes
        WHERE review_id = #{reviewId}
    </select>

    <insert id="insertReviewLike" useGeneratedKeys="true" keyProperty="reviewLikeId">
        INSERT INTO review_likes (review_id, user_id, created_at, updated_at)
        VALUES (#{reviewId}, #{userId}, NOW(), NOW())
    </insert>

    <delete id="deleteReviewLikeByReviewIdAndUserId">
        DELETE FROM review_likes
        WHERE review_id = #{reviewId}
          AND user_id = #{userId}
    </delete>

    <!-- ═══ ReviewVote ═══ -->

    <select id="findReviewVoteByUserIdAndReviewId" resultMap="reviewVoteResultMap">
        SELECT review_vote_id, user_id, review_id, helpful, created_at, updated_at
        FROM review_votes
        WHERE user_id = #{userId}
          AND review_id = #{reviewId}
        LIMIT 1
    </select>

    <select id="countReviewVoteByReviewIdAndHelpfulTrue" resultType="long">
        SELECT COUNT(*)
        FROM review_votes
        WHERE review_id = #{reviewId}
          AND helpful = true
    </select>

    <select id="countReviewVoteByReviewIdAndHelpfulFalse" resultType="long">
        SELECT COUNT(*)
        FROM review_votes
        WHERE review_id = #{reviewId}
          AND helpful = false
    </select>

    <insert id="insertReviewVote" useGeneratedKeys="true" keyProperty="reviewVoteId">
        INSERT INTO review_votes (user_id, review_id, helpful, created_at, updated_at)
        VALUES (#{userId}, #{reviewId}, #{helpful}, NOW(), NOW())
    </insert>

    <update id="updateReviewVoteHelpful">
        UPDATE review_votes
        SET helpful = #{helpful},
            updated_at = NOW()
        WHERE review_vote_id = #{reviewVoteId}
    </update>

    <!-- ══════════════════════════════════════════════ -->
    <!-- 관리자 통계용 집계 쿼리 (AdminStatsService 섹션 13, 14) -->
    <!-- ══════════════════════════════════════════════ -->

    <!--
        review_category_code 별 리뷰 건수 집계.
        소프트 삭제(is_deleted=true) 제외.
        반환: [{categoryCode, cnt}] — cnt 내림차순.
        AdminStatsService.getReviewQualityStats() 에서 사용.
    -->
    <select id="countGroupByCategory" resultType="java.util.LinkedHashMap">
        SELECT
            COALESCE(review_category_code, 'NONE') AS categoryCode,
            COUNT(*)                               AS cnt
        FROM reviews
        WHERE is_deleted = false
        GROUP BY review_category_code
        ORDER BY cnt DESC
    </select>

    <!--
        평점(1~5)별 리뷰 건수 집계.
        소프트 삭제(is_deleted=true) 제외.
        반환: [{rating, cnt}] — rating 오름차순.
        AdminStatsService.getReviewQualityStats() 에서 사용.
    -->
    <select id="countGroupByRating" resultType="java.util.LinkedHashMap">
        SELECT
            rating AS rating,
            COUNT(*) AS cnt
        FROM reviews
        WHERE is_deleted = false
          AND rating IS NOT NULL
        GROUP BY rating
        ORDER BY rating ASC
    </select>

    <!--
        지정 기간 내 리뷰 작성 고유 사용자 수.
        소프트 삭제 레코드도 포함 (퍼널 분석은 발생량 기준).
        AdminStatsService.getFunnelConversion() 전환 퍼널 단계 4 에서 사용.
    -->
    <select id="countDistinctUserByCreatedAtBetween" resultType="long">
        SELECT COUNT(DISTINCT user_id)
        FROM reviews
        WHERE created_at >= #{start}
          AND created_at  &lt; #{end}
    </select>

    <!--
        genre_explorer 업적용 — 사용자가 리뷰한 영화의 고유 장르 수.
        movies.genres JSON 배열(예: ["액션","SF"])을 JSON_TABLE로 확장 후
        DISTINCT 집계. 소프트 삭제된 리뷰 제외.
        MySQL 8.0+ JSON_TABLE 함수 사용.
    -->
    <select id="countDistinctExploredGenres" resultType="long">
        SELECT COUNT(DISTINCT jt.genre)
        FROM reviews r
        JOIN movies m ON r.movie_id = m.movie_id COLLATE utf8mb4_unicode_ci,
        JSON_TABLE(
          CASE
            WHEN JSON_VALID(m.genres) THEN m.genres
            ELSE JSON_ARRAY()
          END,
          '$[*]' COLUMNS (genre VARCHAR(100) PATH '$')
        ) jt
        WHERE r.user_id = #{userId}
          AND r.is_deleted = false
          AND m.genres IS NOT NULL
          AND JSON_VALID(m.genres)
          AND JSON_LENGTH(m.genres) > 0
    </select>

</mapper>
