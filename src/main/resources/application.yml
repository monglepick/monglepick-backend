spring:
  application:
    name: demo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.50.26.166:3306/monglepick?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: monglepick
    password: monglepick
  # JPA ddl, Naming and log
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
  jwt: # 암호화, 복호화 시킬 변수 설정
    secret: vmfhaltmskdlstkfkdgodyroqkfwkdbalroqkfwkdbalaaaaaaaaaaaaaaaabbbbb



  # OAuth2 소셜 로그인 설정
  security:
    oauth2:
      client:
        registration:

          # 네이버 로그인 설정
          naver:
            client-name: naver
            client-id: uQRClbNcYJ8ghG61OOFi
            client-secret: t3AxbdBp2E
            redirect-uri: http://localhost:8080/login/oauth2/code/naver
            authorization-grant-type: authorization_code
            scope:
              - name
              - email

          # 구글 로그인 설정
        #          google:
        #           client-name: google
        #          client-id:
        #         client-secret:
        #        redirect-uri: http://localhost:8080/login/oauth2/code/google
        #       authorization-grant-type: authorization_code
        #      scope:
        #       - profile
        #      - email

        provider:

          # 네이버 OAuth2 엔드포인트 설정
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response  # 네이버 응답 최상위 키


debug: true